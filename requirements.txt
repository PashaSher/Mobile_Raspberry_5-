# Raspberry Pi (передатчик камеры): venv на плате + пакеты ниже
opencv-python-headless>=4.8.0
numpy>=1.24.0
# Быстрый JPEG 4:4:4 на Pi (picamera2 без overlay — см. rpi_tools/camera_stream.py)
simplejpeg>=1.7.1
# CSI / libcamera: sudo apt install -y python3-libcamera libcap2-dev && pip install picamera2
# Автозапуск по GPIO (boot_gpio_gate): pip install gpiozero
gpiozero>=2.0.0
# WebRTC signaling (webrtc) + firebase-probe
firebase-admin>=6.5.0
# WebRTC медиа-треки (webrtc_host, webrtc_video)
aiortc>=1.9.0
av>=12.0.0
# Автозапуск с ожиданием отладчика (scripts/camstream-gpio-gate-debug.service.example)
debugpy>=1.8.0
